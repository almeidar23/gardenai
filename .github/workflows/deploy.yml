name: Deploy to Bluehost

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger deploy webhook
        run: |
          curl -s "https://7ire.com/gardenai/deploy.php?token=${{ secrets.DEPLOY_SECRET }}"
